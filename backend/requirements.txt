# 披呦後端依賴 / Piyou Backend Dependencies
bcrypt>=4.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
requests>=2.32.0
beautifulsoup4>=4.13.0
pyjwt>=2.10.0
python-dotenv>=1.0.0
pydantic>=2.10.0

# PostgreSQL 主要儲存後端 / Primary PostgreSQL Storage Backend
asyncpg>=0.29.0

# Google Sheets 儲存後端（舊版，保留備用）/ Legacy Sheets backend (kept for reference)
# STORAGE_BACKEND=sheets 需要 / required for sheets:
google-api-python-client>=2.100.0
google-auth>=2.23.0

# PWA 推播通知 / PWA Push Notifications
pywebpush>=2.0.0

# 測試依賴 / Test Dependencies
pytest>=8.0.0
httpx>=0.27.0
pytest-asyncio>=0.24.0
